# App
APP_NAME=autodashboard-ventas
APP_ENV=dev
SECRET_KEY=CAMBIAME_POR_UNO_LARGO_Y_ALEATORIO

# Logging (overridea logging.level de YAML si tu settings lo permite)
LOG_LEVEL=INFO

# Uploads / UI / CSV
ALLOWED_FILE_TYPES=csv,xlsx,json
MAX_UPLOAD_MB=15
CSV_ENCODING=utf-8
STREAMLIT_SERVER_PORT=8501

# ===== DEV DB =====
DB_USER_DEV=usuario_dev
DB_PASS_DEV=clave_dev
DB_HOST_DEV=localhost
DB_PORT_DEV=5432
DB_NAME_DEV=auto_dashboard_dev

# ===== PROD DB (placeholders, NO claves reales) =====
DB_USER=usuario_prod
DB_PASS=clave_prod
DB_HOST=midominio.com
DB_PORT=5432
DB_NAME=auto_dashboard_prod

# Features (usa true/false de forma consistente)
ENABLE_EXPERIMENTAL=true